# Project workflow

- Keep `main` review-only. Do not push implementation commits directly to it.
- Create a focused feature branch for every iteration.
- Build and test the branch before publishing it.
- Push the branch and open a pull request for user review.
- Do not merge a pull request unless the user explicitly asks for the merge.
